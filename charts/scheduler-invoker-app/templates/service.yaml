apiVersion: v1
kind: Service
metadata:
  name: {{ include "scheduler-invoker-app.fullname" . }}
  labels:
    {{- include "scheduler-invoker-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scheduler-invoker-app.selectorLabels" . | nindent 4 }}
